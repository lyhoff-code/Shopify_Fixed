<!-- Animated Effects: Icons, Confetti, Loading, Gamification - Modern 3D/4D -->

<!-- Modern 3D Loading Screen -->
<div id="page-loader" class="page-loader-3d">
  <div class="loader-scene">
    <!-- 3D Rotating cube background -->
    <div class="loader-cubes">
      <div class="cube cube-1"></div>
      <div class="cube cube-2"></div>
      <div class="cube cube-3"></div>
    </div>

    <div class="loader-content-3d">
      <!-- Animated Doctor Icon -->
      <div class="loader-avatar-3d">
        <div class="avatar-ring"></div>
        <div class="avatar-ring ring-2"></div>
        <div class="avatar-inner">
          <span class="avatar-emoji">👨‍⚕️</span>
        </div>
      </div>

      <!-- Brand name with glow -->
      <h2 class="loader-brand">Organic Skincare</h2>

      <!-- Loading message -->
      <p class="loader-message">Dr. James is preparing your experience...</p>

      <!-- Modern progress bar -->
      <div class="loader-progress-3d">
        <div class="progress-track">
          <div class="progress-fill"></div>
          <div class="progress-glow"></div>
        </div>
        <div class="progress-dots">
          <span class="dot"></span>
          <span class="dot"></span>
          <span class="dot"></span>
        </div>
      </div>

      <!-- Floating particles -->
      <div class="loader-particles">
        <span class="particle p1">✨</span>
        <span class="particle p2">🧴</span>
        <span class="particle p3">💧</span>
        <span class="particle p4">🌿</span>
        <span class="particle p5">💫</span>
      </div>
    </div>
  </div>
</div>

<!-- Confetti Canvas -->
<canvas id="confetti-canvas" class="confetti-canvas"></canvas>

<!-- Rewards/Gamification Panel -->
<div id="rewards-panel" class="rewards-panel">
  <button class="rewards-toggle">
    <span class="rewards-icon">🎁</span>
    <span class="rewards-points" id="reward-points">0</span>
  </button>

  <div class="rewards-dropdown">
    <div class="rewards-header">
      <h4>🌿 Your Rewards</h4>
      <p>Earn points, get free products!</p>
    </div>

    <div class="rewards-balance">
      <span class="rewards-balance-number" id="rewards-balance">0</span>
      <span class="rewards-balance-label">Points</span>
    </div>

    <div class="rewards-progress-tier">
      <div class="tier-labels">
        <span>Seed 🌱</span>
        <span>Sprout 🌿</span>
        <span>Bloom 🌸</span>
        <span>Tree 🌳</span>
      </div>
      <div class="tier-progress">
        <div class="tier-progress-bar" id="tier-progress"></div>
      </div>
      <p class="tier-status" id="tier-status">150 points to Sprout 🌿</p>
    </div>

    <div class="rewards-ways">
      <h5>Ways to Earn</h5>
      <div class="reward-way">
        <span class="reward-way-icon">🛒</span>
        <span class="reward-way-text">Purchase</span>
        <span class="reward-way-points">1pt / $1</span>
      </div>
      <div class="reward-way">
        <span class="reward-way-icon">📝</span>
        <span class="reward-way-text">Write a Review</span>
        <span class="reward-way-points">+50 pts</span>
      </div>
      <div class="reward-way">
        <span class="reward-way-icon">👥</span>
        <span class="reward-way-text">Refer a Friend</span>
        <span class="reward-way-points">+200 pts</span>
      </div>
      <div class="reward-way">
        <span class="reward-way-icon">🎂</span>
        <span class="reward-way-text">Birthday Bonus</span>
        <span class="reward-way-points">+100 pts</span>
      </div>
    </div>

    <div class="rewards-redeem">
      <h5>Redeem Points</h5>
      <div class="redeem-option" data-points="100">
        <span>$5 Off</span>
        <button class="redeem-btn">100 pts</button>
      </div>
      <div class="redeem-option" data-points="200">
        <span>$15 Off</span>
        <button class="redeem-btn">200 pts</button>
      </div>
      <div class="redeem-option" data-points="500">
        <span>Free Product</span>
        <button class="redeem-btn">500 pts</button>
      </div>
    </div>
  </div>
</div>

<!-- Animated Icons Library -->
<div style="display: none;">
  <svg id="animated-icons">
    <!-- Heart Icon -->
    <symbol id="icon-heart" viewBox="0 0 24 24">
      <path class="heart-path" d="M12 21C12 21 4 13.5 4 8.5C4 5.5 6.5 3 9.5 3C11 3 12 4 12 4C12 4 13 3 14.5 3C17.5 3 20 5.5 20 8.5C20 13.5 12 21 12 21Z" fill="currentColor"/>
    </symbol>

    <!-- Cart Icon -->
    <symbol id="icon-cart" viewBox="0 0 24 24">
      <circle class="cart-wheel" cx="9" cy="21" r="1"/>
      <circle class="cart-wheel" cx="20" cy="21" r="1"/>
      <path class="cart-body" d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/>
    </symbol>

    <!-- Star Icon -->
    <symbol id="icon-star" viewBox="0 0 24 24">
      <polygon class="star-path" points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
    </symbol>

    <!-- Leaf Icon -->
    <symbol id="icon-leaf" viewBox="0 0 24 24">
      <path class="leaf-path" d="M12 2C6.5 2 2 6.5 2 12c0 5.5 4.5 10 10 10 1.5 0 3-.3 4.3-.9M12 2v10m0 0l6-6M12 12l4 4"/>
      <path d="M17 2c3 3 5 7 5 10 0 2-1 4-2 5.5"/>
    </symbol>
  </svg>
</div>

<style>
/* Modern 3D Page Loader */
.page-loader-3d {
  position: fixed;
  inset: 0;
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
  z-index: 99999;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: opacity 0.6s ease, visibility 0.6s ease;
  perspective: 1000px;
  overflow: hidden;
}

.page-loader-3d.loaded {
  opacity: 0;
  visibility: hidden;
}

.loader-scene {
  position: relative;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 3D Floating cubes background */
.loader-cubes {
  position: absolute;
  inset: 0;
  overflow: hidden;
}

.cube {
  position: absolute;
  width: 60px;
  height: 60px;
  background: linear-gradient(135deg, rgba(45,90,74,0.3) 0%, rgba(45,90,74,0.1) 100%);
  border: 1px solid rgba(45,90,74,0.3);
  border-radius: 10px;
  animation: floatCube 8s ease-in-out infinite;
}

.cube-1 { top: 20%; left: 10%; animation-delay: 0s; }
.cube-2 { top: 60%; right: 15%; animation-delay: 2s; width: 40px; height: 40px; }
.cube-3 { bottom: 25%; left: 20%; animation-delay: 4s; width: 80px; height: 80px; }

@keyframes floatCube {
  0%, 100% { transform: translateY(0) rotateX(0deg) rotateY(0deg); opacity: 0.5; }
  50% { transform: translateY(-30px) rotateX(180deg) rotateY(180deg); opacity: 0.8; }
}

.loader-content-3d {
  text-align: center;
  z-index: 10;
  position: relative;
}

/* 3D Avatar */
.loader-avatar-3d {
  position: relative;
  width: 120px;
  height: 120px;
  margin: 0 auto 30px;
}

.avatar-ring {
  position: absolute;
  inset: 0;
  border: 3px solid rgba(45,90,74,0.5);
  border-radius: 50%;
  animation: avatarRing 2s ease-out infinite;
}

.avatar-ring.ring-2 {
  animation-delay: 1s;
}

@keyframes avatarRing {
  0% { transform: scale(1); opacity: 1; }
  100% { transform: scale(1.8); opacity: 0; }
}

.avatar-inner {
  position: absolute;
  inset: 10px;
  background: linear-gradient(145deg, #2D5A4A, #3d7a64);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 10px 40px rgba(45,90,74,0.5), inset 0 -5px 20px rgba(0,0,0,0.2);
  animation: avatarPulse 2s ease-in-out infinite;
}

@keyframes avatarPulse {
  0%, 100% { transform: scale(1) rotateY(0deg); }
  50% { transform: scale(1.05) rotateY(10deg); }
}

.avatar-emoji {
  font-size: 50px;
  animation: emojiFloat 3s ease-in-out infinite;
}

@keyframes emojiFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-5px); }
}

.loader-brand {
  font-size: 1.8rem;
  font-weight: 300;
  color: #fff;
  margin-bottom: 10px;
  letter-spacing: 3px;
  text-transform: uppercase;
  text-shadow: 0 0 30px rgba(45,90,74,0.5);
}

.loader-message {
  color: rgba(255,255,255,0.7);
  font-size: 0.95rem;
  margin-bottom: 30px;
  animation: messageFade 2s ease-in-out infinite;
}

@keyframes messageFade {
  0%, 100% { opacity: 0.7; }
  50% { opacity: 1; }
}

/* 3D Progress bar */
.loader-progress-3d {
  width: 250px;
  margin: 0 auto;
}

.progress-track {
  height: 6px;
  background: rgba(255,255,255,0.1);
  border-radius: 3px;
  overflow: hidden;
  position: relative;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #2D5A4A, #4FC3F7, #2D5A4A);
  background-size: 200% 100%;
  border-radius: 3px;
  animation: progressFill 2s ease-in-out infinite;
}

@keyframes progressFill {
  0% { width: 0%; background-position: 0% 0%; }
  50% { width: 70%; background-position: 100% 0%; }
  100% { width: 100%; background-position: 0% 0%; }
}

.progress-glow {
  position: absolute;
  top: -5px;
  left: 0;
  width: 30%;
  height: 16px;
  background: linear-gradient(90deg, transparent, rgba(45,90,74,0.5), transparent);
  border-radius: 50%;
  animation: progressGlow 2s ease-in-out infinite;
}

@keyframes progressGlow {
  0% { left: -30%; }
  100% { left: 100%; }
}

.progress-dots {
  display: flex;
  justify-content: center;
  gap: 8px;
  margin-top: 15px;
}

.dot {
  width: 8px;
  height: 8px;
  background: rgba(45,90,74,0.5);
  border-radius: 50%;
  animation: dotPulse 1.5s ease-in-out infinite;
}

.dot:nth-child(2) { animation-delay: 0.2s; }
.dot:nth-child(3) { animation-delay: 0.4s; }

@keyframes dotPulse {
  0%, 100% { transform: scale(1); background: rgba(45,90,74,0.5); }
  50% { transform: scale(1.3); background: #2D5A4A; }
}

/* Floating particles */
.loader-particles {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.particle {
  position: absolute;
  font-size: 20px;
  animation: particleFloat 6s ease-in-out infinite;
}

.p1 { top: 15%; left: 20%; animation-delay: 0s; }
.p2 { top: 25%; right: 25%; animation-delay: 1s; }
.p3 { bottom: 30%; left: 15%; animation-delay: 2s; }
.p4 { bottom: 20%; right: 20%; animation-delay: 3s; }
.p5 { top: 50%; left: 5%; animation-delay: 4s; }

@keyframes particleFloat {
  0%, 100% { transform: translateY(0) rotate(0deg); opacity: 0.4; }
  50% { transform: translateY(-40px) rotate(180deg); opacity: 0.8; }
}

/* Legacy support */
.loader-mascot {
  width: 80px;
  height: 95px;
  margin: 0 auto 20px;
  animation: loaderBounce 1s ease-in-out infinite;
}

@keyframes loaderBounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-15px); }
}

.loader-leaf {
  width: 100%;
  height: 100%;
}

.leaf-body {
  animation: leafPulse 1.5s ease-in-out infinite;
}

@keyframes leafPulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

.loader-smile {
  animation: loaderSmile 2s ease-in-out infinite;
}

@keyframes loaderSmile {
  0%, 100% { d: path("M50 90 Q60 100 70 90"); }
  50% { d: path("M50 85 Q60 105 70 85"); }
}

.loader-text {
  font-size: 1rem;
  color: #666;
  margin-bottom: 20px;
  animation: loaderFade 1.5s ease-in-out infinite;
}

@keyframes loaderFade {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.loader-progress {
  width: 200px;
  height: 4px;
  background: #e0e0e0;
  border-radius: 2px;
  overflow: hidden;
  margin: 0 auto;
}

.loader-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #2D5A4A, #3d7a64, #2D5A4A);
  background-size: 200% 100%;
  animation: loaderProgress 1.5s ease-in-out infinite;
  width: 100%;
}

@keyframes loaderProgress {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* Confetti Canvas */
.confetti-canvas {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 99998;
}

/* Modern 3D Rewards Panel */
.rewards-panel {
  position: fixed;
  top: 120px;
  right: 0;
  z-index: 9995;
  perspective: 1000px;
}

.rewards-toggle {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding: 12px 10px;
  background: linear-gradient(135deg, #FFD700, #FFA500);
  border: none;
  border-radius: 12px 0 0 12px;
  cursor: pointer;
  box-shadow: -4px 0 15px rgba(0,0,0,0.1);
  transition: all 0.3s ease;
}

.rewards-toggle:hover {
  padding-right: 15px;
}

.rewards-icon {
  font-size: 1.5rem;
  animation: rewardsBounce 2s ease-in-out infinite;
}

@keyframes rewardsBounce {
  0%, 100% { transform: rotate(0deg); }
  25% { transform: rotate(-10deg); }
  75% { transform: rotate(10deg); }
}

.rewards-points {
  font-size: 0.75rem;
  font-weight: 700;
  color: #1A1A1A;
  background: #fff;
  padding: 2px 8px;
  border-radius: 10px;
}

.rewards-dropdown {
  position: absolute;
  right: 100%;
  top: 50%;
  transform: translateY(-50%) translateX(20px);
  width: 300px;
  background: #fff;
  border-radius: 16px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.15);
  padding: 20px;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.rewards-panel:hover .rewards-dropdown,
.rewards-panel.open .rewards-dropdown {
  opacity: 1;
  visibility: visible;
  transform: translateY(-50%) translateX(0);
}

.rewards-header {
  text-align: center;
  margin-bottom: 20px;
}

.rewards-header h4 {
  margin: 0 0 5px 0;
  font-size: 1.25rem;
}

.rewards-header p {
  margin: 0;
  color: #666;
  font-size: 0.85rem;
}

.rewards-balance {
  text-align: center;
  padding: 20px;
  background: linear-gradient(135deg, rgba(45,90,74,0.1), rgba(45,90,74,0.05));
  border-radius: 12px;
  margin-bottom: 20px;
}

.rewards-balance-number {
  display: block;
  font-size: 2.5rem;
  font-weight: 700;
  color: #2D5A4A;
  line-height: 1;
}

.rewards-balance-label {
  font-size: 0.85rem;
  color: #666;
}

.rewards-progress-tier {
  margin-bottom: 20px;
}

.tier-labels {
  display: flex;
  justify-content: space-between;
  font-size: 0.7rem;
  margin-bottom: 8px;
}

.tier-progress {
  height: 8px;
  background: #e0e0e0;
  border-radius: 4px;
  overflow: hidden;
}

.tier-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #8BC34A, #2D5A4A);
  border-radius: 4px;
  width: 25%;
  transition: width 0.5s ease;
}

.tier-status {
  text-align: center;
  font-size: 0.8rem;
  color: #666;
  margin-top: 8px;
}

.rewards-ways,
.rewards-redeem {
  margin-bottom: 15px;
}

.rewards-ways h5,
.rewards-redeem h5 {
  font-size: 0.9rem;
  margin: 0 0 10px 0;
  color: #1A1A1A;
}

.reward-way {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 0;
  border-bottom: 1px solid #f0f0f0;
}

.reward-way:last-child {
  border-bottom: none;
}

.reward-way-icon {
  font-size: 1.25rem;
}

.reward-way-text {
  flex: 1;
  font-size: 0.85rem;
}

.reward-way-points {
  font-size: 0.8rem;
  font-weight: 600;
  color: #2D5A4A;
}

.redeem-option {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 0;
  border-bottom: 1px solid #f0f0f0;
}

.redeem-option:last-child {
  border-bottom: none;
}

.redeem-btn {
  padding: 6px 12px;
  background: #2D5A4A;
  color: #fff;
  border: none;
  border-radius: 15px;
  font-size: 0.75rem;
  cursor: pointer;
  transition: all 0.3s ease;
}

.redeem-btn:hover {
  background: #3d7a64;
  transform: scale(1.05);
}

.redeem-btn:disabled {
  background: #ccc;
  cursor: not-allowed;
}

/* Animated Icon Styles */
.animated-icon {
  transition: all 0.3s ease;
}

.animated-icon:hover {
  transform: scale(1.2);
}

.animated-icon.heart:hover {
  animation: heartBeat 0.6s ease-in-out;
}

@keyframes heartBeat {
  0%, 100% { transform: scale(1); }
  25% { transform: scale(1.3); }
  50% { transform: scale(1); }
  75% { transform: scale(1.2); }
}

.animated-icon.cart.adding {
  animation: cartAdd 0.5s ease-out;
}

@keyframes cartAdd {
  0% { transform: scale(1); }
  50% { transform: scale(1.3) rotate(-10deg); }
  100% { transform: scale(1) rotate(0); }
}

.animated-icon.star.active {
  animation: starPop 0.4s ease-out;
  color: #FFD700;
}

@keyframes starPop {
  0% { transform: scale(0); }
  50% { transform: scale(1.4); }
  100% { transform: scale(1); }
}

/* Success/Celebration Effects */
.success-checkmark {
  width: 80px;
  height: 80px;
  margin: 0 auto;
}

.success-checkmark svg {
  width: 100%;
  height: 100%;
}

.checkmark-circle {
  stroke: #2D5A4A;
  stroke-width: 2;
  fill: none;
  stroke-dasharray: 166;
  stroke-dashoffset: 166;
  animation: checkmarkCircle 0.6s ease-in-out forwards;
}

@keyframes checkmarkCircle {
  to { stroke-dashoffset: 0; }
}

.checkmark-check {
  stroke: #2D5A4A;
  stroke-width: 3;
  fill: none;
  stroke-dasharray: 48;
  stroke-dashoffset: 48;
  animation: checkmarkCheck 0.3s ease-in-out 0.6s forwards;
}

@keyframes checkmarkCheck {
  to { stroke-dashoffset: 0; }
}

/* Mobile */
@media (max-width: 768px) {
  .rewards-panel {
    top: auto;
    bottom: 80px;
    right: 10px;
    transform: none;
  }

  .rewards-toggle {
    border-radius: 12px;
    padding: 10px;
  }

  .rewards-dropdown {
    right: 0;
    top: auto;
    bottom: 100%;
    transform: translateY(-10px);
    width: 280px;
  }

  .rewards-panel:hover .rewards-dropdown,
  .rewards-panel.open .rewards-dropdown {
    transform: translateY(0);
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Page Loader
  const loader = document.getElementById('page-loader');
  window.addEventListener('load', () => {
    setTimeout(() => {
      loader.classList.add('loaded');
    }, 800);
  });

  // Confetti System
  const confettiCanvas = document.getElementById('confetti-canvas');
  const confettiCtx = confettiCanvas.getContext('2d');
  let confettiParticles = [];

  function resizeConfettiCanvas() {
    confettiCanvas.width = window.innerWidth;
    confettiCanvas.height = window.innerHeight;
  }
  resizeConfettiCanvas();
  window.addEventListener('resize', resizeConfettiCanvas);

  class ConfettiParticle {
    constructor(x, y) {
      this.x = x;
      this.y = y;
      this.size = Math.random() * 10 + 5;
      this.speedY = Math.random() * 3 + 2;
      this.speedX = (Math.random() - 0.5) * 4;
      this.rotation = Math.random() * 360;
      this.rotationSpeed = (Math.random() - 0.5) * 10;
      this.color = ['#2D5A4A', '#3d7a64', '#8BC34A', '#FFD700', '#FF6B6B', '#9B59B6'][Math.floor(Math.random() * 6)];
      this.shape = Math.random() > 0.5 ? 'rect' : 'circle';
      this.opacity = 1;
    }

    update() {
      this.y += this.speedY;
      this.x += this.speedX;
      this.rotation += this.rotationSpeed;
      this.speedY += 0.1;
      this.opacity -= 0.01;
    }

    draw() {
      confettiCtx.save();
      confettiCtx.translate(this.x, this.y);
      confettiCtx.rotate(this.rotation * Math.PI / 180);
      confettiCtx.globalAlpha = this.opacity;
      confettiCtx.fillStyle = this.color;

      if (this.shape === 'rect') {
        confettiCtx.fillRect(-this.size / 2, -this.size / 4, this.size, this.size / 2);
      } else {
        confettiCtx.beginPath();
        confettiCtx.arc(0, 0, this.size / 2, 0, Math.PI * 2);
        confettiCtx.fill();
      }

      confettiCtx.restore();
    }
  }

  function createConfetti(x, y, count = 100) {
    for (let i = 0; i < count; i++) {
      confettiParticles.push(new ConfettiParticle(
        x + (Math.random() - 0.5) * 100,
        y
      ));
    }
    animateConfetti();
  }

  function animateConfetti() {
    confettiCtx.clearRect(0, 0, confettiCanvas.width, confettiCanvas.height);

    confettiParticles = confettiParticles.filter(p => p.opacity > 0 && p.y < confettiCanvas.height);

    confettiParticles.forEach(p => {
      p.update();
      p.draw();
    });

    if (confettiParticles.length > 0) {
      requestAnimationFrame(animateConfetti);
    }
  }

  // Expose confetti function globally
  window.triggerConfetti = (x = window.innerWidth / 2, y = 0) => {
    createConfetti(x, y);
  };

  // Rewards System
  const rewardsPanel = document.getElementById('rewards-panel');
  const rewardsToggle = rewardsPanel.querySelector('.rewards-toggle');
  let rewardsPoints = parseInt(localStorage.getItem('rewardsPoints') || '0');

  function updateRewardsDisplay() {
    document.getElementById('reward-points').textContent = rewardsPoints;
    document.getElementById('rewards-balance').textContent = rewardsPoints;

    // Update tier progress
    const tierProgress = document.getElementById('tier-progress');
    const tierStatus = document.getElementById('tier-status');

    let progress = 0;
    let statusText = '';

    if (rewardsPoints < 100) {
      progress = (rewardsPoints / 100) * 25;
      statusText = `${100 - rewardsPoints} points to Sprout 🌿`;
    } else if (rewardsPoints < 300) {
      progress = 25 + ((rewardsPoints - 100) / 200) * 25;
      statusText = `${300 - rewardsPoints} points to Bloom 🌸`;
    } else if (rewardsPoints < 600) {
      progress = 50 + ((rewardsPoints - 300) / 300) * 25;
      statusText = `${600 - rewardsPoints} points to Tree 🌳`;
    } else {
      progress = 100;
      statusText = `You're a Tree! 🌳 Maximum rewards!`;
    }

    tierProgress.style.width = progress + '%';
    tierStatus.textContent = statusText;

    // Update redeem buttons
    rewardsPanel.querySelectorAll('.redeem-btn').forEach(btn => {
      const points = parseInt(btn.closest('.redeem-option').dataset.points);
      btn.disabled = rewardsPoints < points;
    });

    localStorage.setItem('rewardsPoints', rewardsPoints);
  }

  // Add points function
  window.addRewardsPoints = (points, showConfetti = true) => {
    rewardsPoints += points;
    updateRewardsDisplay();

    if (showConfetti && points >= 50) {
      window.triggerConfetti();
    }

    // Show notification
    showPointsNotification(points);
  };

  function showPointsNotification(points) {
    const notification = document.createElement('div');
    notification.className = 'points-notification';
    notification.innerHTML = `+${points} points! 🎉`;
    notification.style.cssText = `
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: linear-gradient(135deg, #2D5A4A, #3d7a64);
      color: #fff;
      padding: 20px 40px;
      border-radius: 30px;
      font-size: 1.5rem;
      font-weight: 700;
      z-index: 99999;
      animation: pointsPopIn 0.5s ease-out, pointsPopOut 0.3s ease-in 1.5s forwards;
    `;

    document.body.appendChild(notification);
    setTimeout(() => notification.remove(), 2000);
  }

  // Add animation styles
  const style = document.createElement('style');
  style.textContent = `
    @keyframes pointsPopIn {
      from { transform: translate(-50%, -50%) scale(0); opacity: 0; }
      to { transform: translate(-50%, -50%) scale(1); opacity: 1; }
    }
    @keyframes pointsPopOut {
      to { transform: translate(-50%, -50%) scale(0); opacity: 0; }
    }
  `;
  document.head.appendChild(style);

  // Toggle rewards panel on mobile
  rewardsToggle.addEventListener('click', () => {
    rewardsPanel.classList.toggle('open');
  });

  // Redeem points
  rewardsPanel.querySelectorAll('.redeem-btn').forEach(btn => {
    btn.addEventListener('click', function() {
      const option = this.closest('.redeem-option');
      const points = parseInt(option.dataset.points);

      if (rewardsPoints >= points) {
        rewardsPoints -= points;
        updateRewardsDisplay();
        alert('Reward redeemed! Check your email for the discount code. 🎉');
      }
    });
  });

  // Initialize
  updateRewardsDisplay();

  // Demo: Add points for various actions
  window.addEventListener('productAdded', () => {
    window.addRewardsPoints(10);
  });

  // Add some demo points after a delay
  setTimeout(() => {
    if (rewardsPoints === 0) {
      window.addRewardsPoints(25, false);
    }
  }, 5000);
});
</script>
